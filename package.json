{
  "name": "glass-css-framework",
  "version": "0.0.1",
  "description": "Glass CSS. A simple, lightweight, mostly clear CSS framework.",
  "main": "src/style.scss",
  "scripts": {
    "test": "true-cli tests/tests.scss && notify -m 'All tests passed.' || notify -m 'Test/s failed.' -s Basso --icon https://cdn0.iconfinder.com/data/icons/shift-free/32/Error-128.png",
    "clean": "rm -f dist/*",
    "prescss": "mkdir -p dist && npm run clean",
    "scss": "node-sass --output-style compressed src/style.scss > dist/style-min.css && node-sass --output-style expanded src/style.scss > dist/style.css",
    "autoprefixer": "postcss -u autoprefixer -r dist/*.css",
    "serve": "browser-sync start --server --files 'dist/*.css, *.html'",
    "build:css": "npm test && npm run scss && npm run autoprefixer",
    "build": "npm run build:css",
    "watch:css": "onchange '**/*.scss' -- npm run build:css",
    "watch": "npm run build && parallelshell 'npm run serve' 'npm run watch:css'"
  },
  "author": "Sam Davis <sam@sjd.co>",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^6.4.1",
    "browser-sync": "^2.15.0",
    "node-notifier": "^4.6.1",
    "node-sass": "^3.9.3",
    "onchange": "^3.0.0",
    "parallelshell": "^2.0.0",
    "postcss-cli": "^2.6.0",
    "rename": "^1.0.3",
    "sass-true": "^2.1.3"
  }
}
